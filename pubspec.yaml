name: an_noor
description: An-Noor — Enterprise-Grade Islamic Prayer Times & Calendar App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  bloc: ^8.1.3

  # Dependency Injection
  get_it: ^7.7.0

  # Prayer Times Calculation (null-safe version)
  adhan: ^2.0.0+1

  # Location
  geolocator: ^12.0.0
  geocoding: ^3.0.0

  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.3

  # Calendar
  table_calendar: ^3.1.2
  hijri: ^3.0.0

  # Notifications
  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4

  # Audio
  just_audio: ^0.9.40

  # UI / Animations
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0

  # Routing
  go_router: ^14.2.7

  # Utils
  dartz: ^0.10.1
  intl: ^0.19.0
  flutter_compass: ^0.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.12

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/lottie/
    - assets/images/
